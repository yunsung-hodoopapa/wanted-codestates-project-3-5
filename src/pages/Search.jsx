import React from 'react';
import SearchContainer from '../components/SearchContainer';

const Search = () => {
  return (
    <div>
      <SearchContainer />
    </div>
  );
};

export default Search;
